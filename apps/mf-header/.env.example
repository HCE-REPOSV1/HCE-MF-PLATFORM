# mf-header — Microfrontend de Header (cabecera)
# Copiar a .env y completar con las URLs del entorno de destino.
VITE_REMOTE_SHELL=http://localhost:10500/assets/remoteEntry.js

# ── Gateway (API) ──────────────────────────────────────────
# URLs de los dos API Gateways usados para resolver el practitioner (nombre, foto).
VITE_APIGW_CNL_CROSS=http://localhost:10601
VITE_APIGW_CNL_WEB_EMERGENCY=http://localhost:10602