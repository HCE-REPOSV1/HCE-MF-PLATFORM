# mf-triage — Microfrontend de Triaje
# Copiar a .env y completar con las URLs del entorno de destino.
VITE_REMOTE_SHELL=http://localhost:10500/assets/remoteEntry.js

# ── Gateway (API) ──────────────────────────────────────────
# apigw-cnl-web-emergency (puerto 10602 en local) — proxea también triage/*
# hacia ms-cnl-web-hce-triage. No existe un apigw dedicado a triage todavía.
VITE_APIGW_CNL_WEB_EMERGENCY=http://localhost:10602